SELECT users.id, users.first_name, users.last_name, friends.id, friends.first_name AS Friend_first, friends.last_name AS Friend_last
FROM friendships
LEFT JOIN users
ON users.id = friendships.user_id
LEFT JOIN users AS friends
ON friends.id = friendships.friend_id